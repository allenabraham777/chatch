const EmptyPage = () => {
    return (
        <div className="h-full flex-1 bg-gray-100 flex items-center justify-center">
            <h3 className="text-xl font-sans font-semibold">
                Select a chat or start a conversation.
            </h3>
        </div>
    );
};

export default EmptyPage;
