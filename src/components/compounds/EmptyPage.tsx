const EmptyPage = () => {
    return (
        <div className="hidden sm:flex h-full flex-1 bg-gray-100 items-center justify-center">
            <h3 className="text-xl text-center font-sans font-semibold">
                Select a chat or start a conversation.
            </h3>
        </div>
    );
};

export default EmptyPage;
